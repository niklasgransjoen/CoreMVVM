<Window x:Class="CoreMVVM.Demo.Views.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoreMVVM.Demo.Views"
        xmlns:markup="clr-namespace:CoreMVVM.Windows.Markup;assembly=CoreMVVM.Windows"
        mc:Ignorable="d"
        Title="DialogWindow" Height="450" Width="800">
    <Grid Margin="50" Height="150">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox x:Name="UserInput" Height="25" Text="{markup:StringResource MatrixTruth}" />

        <Button Grid.Row="1" Content="{markup:StringResource ClickMe}" Height="25"
                Command="{Binding ButtonCommand}" CommandParameter="{Binding ElementName=UserInput, Path=Text}" />

        <TextBlock Grid.Row="2" TextAlignment="Center" Text="{Binding Status}" />
    </Grid>
</Window>